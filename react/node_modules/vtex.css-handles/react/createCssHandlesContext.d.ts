import { PropsWithChildren } from 'react';
import type { CssHandlesList, CssHandlesBag } from './CssHandlesTypes';
interface ProviderProps<T extends CssHandlesList> {
    withModifiers: CssHandlesBag<T>['withModifiers'];
    handles: CssHandlesBag<T>['handles'];
}
export default function createCssHandlesContext<T extends CssHandlesList>(_handles: T): {
    CssHandlesProvider: ({ withModifiers, handles, children, }: PropsWithChildren<ProviderProps<T>>) => JSX.Element;
    useContextCssHandles: () => CssHandlesBag<T>;
};
export {};
